# CLAUDE.md

このドキュメントは、プロジェクトのルール、目的、進捗管理方法を定義します。以下の内容に従ってプロジェクトを進めてください。

## 0. 最上位ルール

- 思考は必ず英語で行うこと。ただし、回答は日本語で行う必要があります。
- 絶対に必要な場合を除き、ハードコーディングは避けること。

## 1. リポジトリ構成ルール

- 本リポジトリはモノレポ構成とする
- 実行されるアプリケーションは `apps/` 配下に配置する
- プロンプト・辞書などの共通資産は `packages/` 配下に配置する
- ルートディレクトリに業務ロジックを配置してはならない

```
apps/
web/     # フロントエンド
api/     # バックエンド
packages/
prompts/ # LLM プロンプト・辞書
```

## 2. フロントエンド（Next.js）ルール

- フロントエンドの責務は UI とユーザインタラクションに限定する
- 在庫数の計算・補正・解釈ロジックを実装してはならない
- DB、Supabase、外部永続層を直接操作してはならない
- すべてのデータ操作は FastAPI 経由で行う
- クライアント側で在庫状態を推測・再計算してはならない

## 3. バックエンド（FastAPI）ルール

### 3.1 レイヤ責務

- `api/`
  - HTTP エンドポイント定義のみを行う
  - ビジネスロジックを含めてはならない
- `schemas/`
  - API の入出力スキーマを Pydantic で定義する
- `services/`
  - 自然言語の解釈
  - 在庫イベントの生成
  - 在庫更新処理
- `core/`
  - 設定管理
  - 環境変数
  - 共通依存関係

### 3.2 実装規約

- API ハンドラ内に直接 DB 操作を書いてはならない
- LLM 呼び出しは `services` 層に限定する
- 型定義を通らないデータを扱ってはならない
- 副作用（DB 更新、外部 API 呼び出し）は明示的に分離する

## 4. LLM・プロンプト管理ルール

- プロンプトは必ず `packages/prompts/` に配置する
- プロンプトをコード内に直接記述してはならない
- プロンプト変更はコード変更と同等に扱う
- LLM の出力は必ず構造化データとして受け取る

## 5. 在庫管理ルール

- 在庫更新は状態変更ではなく「イベント」として扱う
- 在庫イベントは以下の種別を基本とする：
  - BUY     # 買った
  - USE     # 使った
  - ADJUST  # 数を合わせる（調整用）
- 数量変更は必ずイベントを経由して行う

## 6. データベース運用ルール

- フロントエンドから DB を直接操作してはならない
- バックエンドは DB 実装に過度に依存してはならない
- 将来 Supabase を正とする構成に移行可能な設計を維持する

## 7. 環境変数・設定管理ルール

- 各アプリケーションは自身の `.env` を管理する
- `.env.example` を必ず用意し、内容を最新に保つ
- 秘密情報をリポジトリに含めてはならない

## 8. テスト・検証ルール

- API レベルのテストよりサービス層のテストを優先する
- LLM に依存する処理は決定論的な入力で検証する
- プロンプトの検証対象は文章品質ではなく構造化出力とする

## 9. 変更・拡張時の原則

- 新たな責務は既存レイヤを破壊せず追加する
- 一時的な実装を恒久的な設計として固定してはならない
- 設計上の判断は暗黙にせず明文化する
